# Muka-Focus-Stacking 开发记录

这份文档是开发过程的原始记录：十一个决定性的 bug、每个 bug 的判据与修法、被证伪的假设、
以及一批"试过、无效、别再重复"的负面结果。**它记录的是"为什么代码长成这样"**，
不是使用说明——用法、参数、构建见 [README.md](README.md)。

阅读顺序建议：先看 [README.md](README.md) 的「工作原理」，再回来看这里对应的一节。

---

## 调研：这个领域有什么

| 方案 | 语言/依赖 | 算法路线 | 问题 |
|---|---|---|---|
| **Helicon Focus** | 闭源商业 | 未知 | 付费；Lite 版输出分辨率被限制 |
| **Zerene Stacker** | 闭源商业 | PMax / DMap | 付费 |
| **Affinity Photo / Photoshop** | 闭源商业 | 金字塔 + 堆栈 | 慢、难批处理 |
| **CombineZP** | 老 Delphi，已停更 | 多种 | 无人维护 |
| **[focus-stack](https://github.com/PetteriAimonen/focus-stack)**（Petteri Aimonen） | C++ / OpenCV + OpenCL | **复数小波(DTCWT)** 域取最大系数 + 小波去噪 + **颜色重映射** | 质量公认最好，但依赖 OpenCV/OpenCL；双树复数小波实现极重，难二次开发 |
| **[z-stackr-algo](https://lib.rs/crates/z-stackr-algo)** | Rust | 拉普拉斯金字塔 "Apex" + SML 深度图 "Relief"（引导滤波 / 多重网格） | Rust 生态确有实现，深度场求解走多重网格 V-cycle，偏重 |
| **学术基线** | — | Forster 等 *Complex Wavelets for Extended Depth-of-Field*；Wang & Chang 2011 *Multi-focus Image Fusion Based on Laplacian Pyramid* | 理论依据 |

本项目的路线：**拉普拉斯金字塔多频段融合 + SML 焦点测度 + 正则化深度场**。
质量接近 focus-stack，但纯 Rust、无重型依赖，算法完全可读可改。

### 为什么不做 DTCWT 和颜色重映射

focus-stack 的"颜色重映射"是为了解决"在灰度域融合后，输出像素的灰度值在原始帧里找不到对应颜色"的问题。
我们在拉普拉斯域**直接对 RGB 三通道一起做混合**，每个输出像素都是真实拍摄值的加权组合，
不存在需要重新指派颜色的问题 —— 于是省掉了整套复数小波变换。

---

---

## 十一个决定性的 bug，以及被证伪的假设

有一类区域长期出不来：**光滑物体（树脂/喷漆表面）压在强纹理背景前**。
表现为物体边缘一条宽而虚的过渡带，而背景反而是锐的。
当时把"判断哪些像素有焦点信息"的各种方案试遍了都无效，一度写进了"已知极限"。

后来发现是**实现缺陷**叠加在一起制造了这个现象 —— 前三个是"测度和几何互相污染"，
第四个是"灰度转换逐帧变化"，它们的作用方式完全相同：**让某些帧在这个区域系统性胜出，
而胜出的理由与对焦无关**。而受害者永远是低信噪比的表面。

#### bug 1 — 对齐变换匹配的是"模糊程度"，不是几何

注册用的特征是**梯度幅值**，而梯度幅值恰恰是随对焦变化的量。于是缩放搜索的胜出方式变成了
"把这一帧做得和参考帧一样模糊"，而不是"和参考帧几何对齐"。证据是 NCC 随离参考帧的
对焦距离单调崩塌，且估出过 8 个 cache 像素的**虚假平移**（三脚架拍摄不可能有这种位移）：

| 帧 | 距参考帧 | scale | NCC |
|---|---|---|---|
| 20 | −5 | 1.0000 | 0.9884 |
| 25 | 0（参考） | 1.0000 | 1.0000 |
| 30 | +5 | 1.0010 | 0.9864 |
| 40 | +15 | 1.0025 | **0.9020** |
| 45 | +20 | 1.0040 | **0.8446** |

修复前这两行的 NCC 是 0.55 和 0.39。修法：计算注册特征**之前**先把工作分辨率图像低通
（`--align-focus-blur`，默认 3），让各帧的离焦差异先被抹平、只留几何；再加一级精搜。
修复后 scale 平滑单调 0.9955 → 1.0045，平移 ≤0.7 像素，NCC 0.84–1.00。

这个结果与另一个**对焦无关**的独立测量完全吻合——先重模糊再搜缩放，得到 0.996 → 1.004、
NCC 0.98–1.00。也就是说：**对焦呼吸是真实存在的**（全程约 0.8%），
只是原来的估计器把它和其他东西混在一起了。

#### bug 2 — 焦点测度测在了重采样过的像素上

`warp` 是一次重采样。对 **JPEG 解码后**的图像重采样会跨 8×8 块边界产生**相位相关的伪高频**，
而这个伪信号恰好落在低对比区（正是需要测的地方），量级足以完全主导 argmax。

实测：一块平滑表面上某像素的响应**在 50 帧里恒等于 0**（一个完全平坦的 JPEG 块）；
一旦经过 warp 就变成 0.001–0.007 的伪响应，于是"最锐帧"被推到远端第 39 帧。

修法：焦点测度**在未 warp 的原帧上计算**，然后把**响应场**（本身就是平滑的标量场）
按变换搬到画布坐标。图像本身仍然 warp —— 用于引导滤波和融合。响应来自未重采样的像素，
各帧的数值才可比。实测该像素从"第 39 帧"回到"第 7 帧"。

#### bug 3 — 没有焦点信息的像素也在投票

平坦 JPEG 块上的响应对每一帧都相同（通常恰好是 0），于是**所有帧并列、第 0 帧胜出**。
这个虚构值会穿过中值滤波，再被引导滤波扩散到整个邻域。

修法：把原始响应峰值为 0 的像素标为"无信息"，从邻域填充。

判据必须用**原始响应**，不能用邻域聚合过的置信度：置信度是在 16 像素窗口上聚合的，
一个自身响应恒为 0 的像素照样能从周围结构继承到健康的比值、被判为"有信息"，
然后带着虚构的深度留下 —— 这个错误把一块真实深度 33 的背景压到第 0 帧并整片糊掉。

#### bug 4 — 灰度转换是逐帧做的，于是测度不再跨帧可比

`focus.rs` 开头写着：焦点测度必须**跨帧可比**，因此**禁止任何逐帧归一化**。
而 `RgbImage::to_grayscale_pca` 恰恰是逐帧归一化：它为**每一帧**单独求均值和协方差，
再把该帧投影到自己的第一主成分上。

主成分是单位向量，所以投影增益取决于它与颜色分布的对齐程度。实测这组素材：
主成分向量随帧号**系统性旋转**（`w_r` 0.390 → 0.362、`w_b` 0.704 → 0.721），
灰色像素上的总增益变化 **0.9%**。单看很小，但**能量是幅度的平方**（ML 测度对幅度线性），
于是各帧的测度被乘上了差 1.8% 的系数——而在低信噪比的区域（正是要害）
**所有帧的响应本来就几乎相等**，这 1.8% 就足以决定谁赢，与真实对焦无关。

端到端后果是一个**系统性偏差**，不是随机噪声：

| 检查点 | 真实最锐帧 | `--gray pca`（旧默认） | `--gray luma`（现默认） |
|---|---|---|---|
| 背景墙 | 33 | 17.8 | **33.0** |
| 面部 | 15 | 23.6 | **15.9** |
| 靴子 | 33 | 16.6 | **33.0** |
| 膝盖边缘 | ~29 | 4.5 | 8.0 |

"真实最锐帧"由 `tools/truth.py` 独立测出：直接在解码后的原帧上算局部锐度，
不使用工具自己的任何测度。

修法：**固定权重 Rec.601 亮度**（`--gray luma`，现默认），每一帧用同一组权重。
`--gray pca` 保留可复现旧行为。

注意这与"亮度是否最优"无关——这只关于**每一帧都相同**。
任何逐帧自适应的转换都会以同一种方式破坏这个测度。

#### bug 5 — 接缝处的两块瑕疵：光晕与灰带（同一个根因）

**症状**（用户报告，肉眼可见）：主体与背景的接缝处有**两块**问题。
一是近处物体的边缘显得**变宽、发虚**，像有一圈它自己的像糊在外面；
二是紧贴轮廓的那圈**背景失去纹理**，变成一条平滑的灰带（这一条只看数字看不出来，
必须放大看图）。

**根因在一次测量里就露出来了**：沿轮廓取一条剖面，

| 深度场 | 台阶宽度（52 像素内落在过渡区的像素数） |
|---|---|
| **原始估计（未平滑）** | **1** |
| `--guided-radius 2`（1 遍） | 6 |
| `--guided-radius 3`（2 遍） | 9 |
| **默认 `--guided-radius 6`（3 遍）** | **16+（更宽处约 30）** |

也就是说：**斜坡不是测量出来的，是我自己的平滑造出来的。**
`guided_filter` 用半径 6、三遍，把一个 1–2 像素的台阶摊成了 30 像素的斜坡。
（早先我以为斜坡来自焦点窗口——那个推论是错的：窗口只决定单像素响应，
而响应本来就已经是干净的台阶。）

斜坡里两个表面都不清晰，于是：
- **基带**（颜色/亮度）取自中间帧 → 近处物体的虚焦晕开在它轮廓**之外** → "边缘被放大了一圈"；
- **细节频段**若由窗口内的某一帧给出，那一帧总有一面是清晰的，于是与基带不一致（光晕）；
  若把窗口关掉（`--seam-window`），细节只能来自中间那两帧，两面都不清晰 → **灰带**。

**修法**：`--depth-bilateral`（默认 6，σ=1.5）——把平滑的**权重建立在深度相似度上**，
而不是建立在图像上。±1.5 帧的散斑照旧被平均掉，20 帧的台阶原封不动。

图像引导做不到这件事：实测该轮廓处的亮度梯度峰值只有 **5.0/像素**，
并不比背景浮雕的 **4.0** 或物体自身表面的 **7.0** 更强——两个表面色调太接近，
图像根本没有"宣告"台阶在哪里。深度对比度却毫不含糊：轮廓两侧相差约 **29 帧**，
而物体自身曲率造成的深度变化只有约 **6 帧**（`--edge-refine` 用的就是这个判据）。

**验证**（纹理能量随"离轮廓距离"的变化，`tools/edgeband.py`）：

| 距轮廓 | 参照成品 | 旧默认（窗口全宽） | `--seam-window 0.4` | **`--depth-bilateral 6`** |
|---|---|---|---|---|
| 6 px | 2.08 | 1.94 | 1.53 | **2.19** |
| 10 px | 1.60 | 2.01 | **1.24** | **1.78** |
| 16 px | 1.89 | 3.43 | **1.18** | **2.13** |
| 24 px | 3.72 | 5.08 | **0.74** | **3.94** |
| 30 px | 3.35 | 3.58 | **1.59** | **3.10** |

`--depth-bilateral` 一整条曲线都贴着参照成品；`--seam-window 0.4` 在 16–30 像素处
塌到 0.7–1.6（就是那条灰带）；旧默认在 16–24 像素处反而**高出**参照（3.4–5.1，光晕）。
各区域锐度完全不变（墙面 23.33、膝盖 56.27、叶片 24.44、面部 58.22）。

**走错的一步（记录在此，别再重复）**：先做的是 `--seam-window`
（按深度变化速度收缩候选窗口，默认已改回 0）。它确实压住了光晕指标，
但代价正是那条灰带——因为关掉窗口就等于在斜坡里放弃细节频段。
**这是把症状从一个位置挪到另一个位置，而根因在更好的一层：让深度保持台阶。**

**指标教训**：用一个"低通后与参照求差"的指标（`tools/bloom.py`）时，
它把 `--seam-window` 版本评为最好（9.09 对 13.32）——与目视**相反**。
原因是那个指标在半径 14 上仍未完全滤掉墙面的浮雕，而我们的浮雕本来就比参照强，
于是"保留纹理"的版本反而被记为"差异大"。真正和眼睛一致的是
**纹理能量随离轮廓距离的变化**（`tools/edgeband.py`）与**放大目视**。

#### bug 6 — 融合造出了"任何输入帧都没有的值"（边缘暗侧被压成黑）

**怎么发现的**：用户指了一个具体位置（输出图像素 x2500–3000, y1500–2000），
放大后那里的深色靴子有**斑驳的暗块和暗晕**。但深度场在十个检验点上都准到 0.0±0.4 帧，
工具的逐帧响应（`--debug-point`）也显示该处深度是对的——**问题不在深度，在融合重建**。

**判据（完全不依赖坐标换算，这是关键）**：逐像素取全部 50 帧的最小值，
得到的是"这个像素可能存在的最暗值"。任何融合结果的近黑像素比例**不可能超过它**。

| | 亮度 < 5 的像素 | 亮度 < 10 的像素 |
|---|---|---|
| **硬下限（逐帧最小值）** | **0.3173%** | **0.7721%** |
| 修复前 | **0.8571%** | 1.2099% |
| 修复后 | 0.3476% | 0.7752% |
| 参照成品（AF导出） | 0.3080% | 0.7609% |

**根因**：拉普拉斯金字塔**逐频段取最大**在高对比边缘上过冲——亮侧的过冲和暗侧的负冲
来自**不同的帧**，相加后暗侧落到所有帧的最小值以下，量化成 0。
于是沿着"亮皮肤／黑靴子"这类边界压出一圈黑，在深色区域表现为斑块。

**修法**：`fuse.rs` 新增 `note_range` / `clamp_to_range`——融合时顺带累积**已 warp 帧**的
逐像素 min/max，重建后把结果夹进这个区间（**两侧都夹**，见 ⑨ bug 9）；
`--no-clamp-lo` 关掉下侧，`--no-clamp-range` 两侧都关（复现旧行为）。
这把上面那条判据从"经验观察"变成了**硬约束**。

**更正（第 15 轮）：两侧性质完全不同，上侧夹取是错的。**

第 14 轮查出"主体远侧那条灰带"的深度成因后，带**依然存在**。第 15 轮把它量化出来，
并用去掉夹取做对照，才看清：

| 距轮廓 | 双侧夹（旧默认） | `--no-clamp-range` | AF 成品 | 逐帧上限 |
|---|---|---|---|---|
| 8 px | 3.39 | **8.09** | 7.35 | 8.63 |
| 12 px | 4.52 | **8.52** | 8.80 | 8.39 |
| 18 px | 6.68 | 10.41 | 8.53 | 7.54 |

恢复出来的（8.09）正好等于**把该处背景拍清楚的那一帧**本身的对比（第 34 帧，8.63/8.40），
所以那是**真浮雕**、不是过冲噪声。机制：该带上每个像素的**帧间范围只有约 14 级宽**，
而浮雕的峰谷正好占满整个区间，于是夹取把波峰波谷**削平**（旧默认下 21% 的带内像素顶在边界上）。
目视见 `out/band_4way.png`：旧默认在离轮廓 0–20 px 处浮雕被抹平，就是用户看到的灰带。

**越界是双向的，但只有下侧有害**：

- **低于 lo**：亮暗边界暗侧的拉普拉斯欠冲，产生"比任何帧都黑"的像素——**bug 6 的假黑**
  （这些像素比 lo 低约 11 级）。**必须夹。**
- **高于 hi**：轮廓远侧的浮雕在重建后高出逐像素上限几级，但那正是**背景的细节**。
  **不该夹。**

于是把 `clamp_to_range` 拆成两侧：

| | L<5（假黑） | 带内纹理 @8px |
|---|---|---|
| 双侧夹（旧默认） | 0.3429% | 3.39 |
| **只夹下侧（新默认）** | **0.3428%** | **8.67** |
| 容差 6 | 0.8135% | 8.67 |
| 两侧都不夹 | 0.8135% | 8.67 |
| 逐像素硬下限 | 0.2777% | — |
| AF 成品 | 0.3080% | 7.35 |

**只夹下侧同时拿到两者**：假黑与旧默认一样，带内纹理从 3.39 恢复到 8.67
（上限 8.40、AF 7.35）。区域锐度全面提升、七个区域仍全在参照之上：

| | gradE | 墙面 | 膝盖边 | 平滑身体 | 叶片 | 面部 | 平坦背景 |
|---|---|---|---|---|---|---|---|
| 参照成品 | 5.315 | 19.13 | 47.12 | 47.52 | 20.90 | 52.10 | 28.29 |
| 双侧夹 | 6.436 | 20.59 | 53.13 | 54.05 | 23.48 | 56.15 | 28.83 |
| **只夹下侧** | 6.701 | 22.58 | 55.39 | 55.84 | 23.93 | 57.69 | 32.11 |

代价：近白（>250）像素从 0.00042% 升到 0.01181%（参照成品 0.00227%），绝对量极小。

改动量：25.6% 的像素变动 >1 级、8.4% >4 级、0.76% >16 级，最大 54 级，全图平均 1.42 级
（1 级在 8bit 下不可见）。

**代价（如实记录）**：过冲同时是被梯度指标算作"锐度"的能量，所以那些指标会下降 3–9%：

| | gradE | 墙面 | 膝盖边 | 平滑身体 | 叶片 | 面部 | 平坦背景 |
|---|---|---|---|---|---|---|---|
| 参照成品 | 5.315 | 19.13 | 47.12 | 47.52 | 20.90 | 52.10 | 28.29 |
| 修复前 | 6.902 | 23.33 | 56.27 | 56.65 | 24.44 | 58.23 | 32.14 |
| **修复后** | 6.438 | 21.55 | 53.61 | 54.49 | 23.49 | 56.27 | 29.20 |

**七个区域仍全面高于参照成品。** 目视上（`out/cell_before_after.png`）：斑驳暗块与暗晕消失。

**坐标换算（第 15 轮用两种独立方法复核过，照抄这一条）**：

```
画布 = 输出 + (0, 8) = 参照成品 + (502, 1295)
```

（即 `参照索引 = 输出索引 − (502, 1303)`。第 10 轮写的 `(495,1287)`／`(18,12)` 差 22 像素，
第 14 轮据此得出的"深度台阶偏进墙侧 21 像素"因此是错的。）
对齐参考帧是**第 25 帧**（变换 = 单位阵），与画布共用同一像素网格。

**定位轮廓边缘必须用输出图，不能用帧**：帧里边缘被离焦糊成 16 像素宽的斜坡，
最陡点离真实轮廓 20+ 像素（帧 25 在 y=2862 处最陡点在 2816，真值 2789）。
参照成品的整图互相关不可靠（残差 53），要用局部模板匹配（残差 4.2）。

**走错的方向（别再重复）**：
- 以为"整幅发软"——实测全局高频能量比 **0.994**；8~12 倍目视在平坦区会骗人。
- 以为"深度错了"——该处深度实测是准的（十个检验点平均差 0.0±0.4 帧）。
- 按颜色通道分别处理再合并——实测色度通道判别力只有亮度的 **1/3**
  （墙面 5.4–7.5 对 1.2–2.3），因为这个场景的背景是灰对灰浮雕（`|R−G|<8`、`|G−B|<8`，
  色度上等于零），主体的细节也全是明暗。见 `tools/chanprobe.py`。


（编号 7 在整理时并入了相邻条目，故此处不连续。）

#### bug 8 — 主体光滑面上的颗粒：`--band-power` 的取舍、"按像素自适应"的失败，与"能量聚合"的成功

**症状**：主体（光滑塑料／深色皮革）表面比参照成品"花"；深度场在那里也斑驳
（`|d − median5×5| > 8 帧` 的像素占 **3.41%**，> 16 帧 0.70%；最差的 512 块里 **19.2%**，
全部集中在主体表面，背景墙与轮廓处几乎为 0）。

先分清两件事：

1. **深度斑驳**（`--save-depth` 里那些红黄斑）不是"深度算错"，而是**那里没有信号**。
   离线复刻管线的测度（1px 预滤波、SML、半径 4、**未 warp 的原帧**）在 256 个采样点上：
   与深度场吻合（≤1 帧）59.4%，差 ≥16 帧 17.2%，判别力（峰值/中位）中位仅 **1.57**（噪声级）。
   主因是**先验窗口按"局部深度跨度"张开**：`--prior-window-gain 0` 使 >8 帧 3.41% → **0.78%**、
   >16 帧 0.70% → **0.01%**。**但画面几乎不变**（塑料面高频 2.34 对 2.37，七个区域
   gradE 6.693 对 6.701）——那些地方所有帧看起来都一样，选错帧不改画面。
2. **光滑面颗粒**——这才是肉眼看到的。逐频段取最大（PMax 类）在每个频段各挑一帧，
   在没有真实像素级细节的地方把各帧的噪声混在一起。塑料大面的高频能量是
   **逐帧上限的 217%**（参照成品 184%——它是带修图的成品，本身也超上限，
   所以"超过上限"不等于噪声）。

**取舍曲线**（带内纹理@12px 的逐帧上限约 8.4）：

| | 塑料面颗粒（越低越干净）| 带内纹理（越高越足）|
|---|---|---|
| `--band-power 1` | **0.39** | 3.51 |
| `--band-power 2` | 0.61 | 6.02 |
| `--band-power 4` | 1.13 | 7.32 |
| 参照成品 | 2.01 | 7.69 |
| `--band-power 0`（取最大；默认已改为能量聚合，见下） | 2.37 | **9.02** |
| 上侧也夹（当时还默认关） | 2.18 | 4.67 |

→ 全局旋钮是**单调取舍**，没有免费午餐。（上侧夹取原本在压颗粒，代价是带内细节腰斩。）

**"按像素自适应"的尝试：失败。实现在代码里但默认关闭（`--conf-band`）。**

思路：管线本来就为每个像素算了可靠性，在没信号处退回"深度插值"就能两全。三种信号都试过：

| 门控信号 | 带内纹理@12px |
|---|---|
| 焦响应峰/均值（`--conf-smooth` 平滑后，即 `DepthBuilder` 的 conf）| 8.91 → **2.20** |
| 焦响应峰/均值（原始响应，自己累积）| → **2.09** |
| **深度自身的局部粗糙度**（`1/(1+|d−med5×5(d)|)`）| → **2.93** |

根因是"哪里的输出可信"这个判据在本素材上**顺序是反的**。在**融合实际消费**的那张图上
逐区域实测（画布坐标）：

| 区域 | 坐标 | 中位值 |
|---|---|---|
| 墙浮雕 | 2874,2670 | 5.88 |
| 平坦背景 | 2412,1218 | 5.56 |
| 皮革带 | 2962,3218 | 2.39 |
| **光滑塑料面（应抑制）** | 3062,3168 | **2.34** |
| **轮廓带（必须保留）** | 2874,2795 | **1.67** |
| 面部区域 | 2506,2004 | 1.62 |

**"必须保留"的轮廓带（1.67）反而低于"应当抑制"的塑料面（2.34）。**
这正是 `depth.rs` 注释里那句"这个量实测是反的"的复现。深度粗糙度那条本应可用
（轮廓处是干净台阶、塑料面在跳），但实测带内仍被完全门控——带边缘本来就有 ≥2 帧的跳动。

**成功的那条路（2026-09-21 定案）：不判"哪里该信谁"，而是让"信谁"在空间上连贯——`--band-energy-smooth`。**

上面的 `--band-power` 是**改数值**（把候选平均掉），所以必然拿细节换干净。换一个轴：
**在选赢家之前，先把每个频段的能量在邻域上聚合一下**（`--band-energy-smooth N`，N 为半径）。
赢家是逐像素最大值，在没有信号的频段里 argmax 跟着噪声跳，于是**相邻像素取自不同的帧**
——把若干份独立的噪声并排放在一起，正是光滑面上看到的颗粒。能量先在邻域上聚合，
选择就在邻域尺度上连贯，该频段局部来自同一帧、带的是那一帧自己的微纹理；
有真实信号时锐帧是以"一片"而非"一个像素"取胜，聚合不改变结果。

**但是"处处聚合"也过头**：它同样会覆盖那些**判决本来是对的**像素，把真实纹理抹软。
实测整幅有 29% 的像素被改动 >4 级（均改 3.6 级），墙面浮雕的高频掉了约 15%。
所以加了一个**不用绝对阈值的判别**：看**次优与最优的能量比**（`runner_up / best`）——
有真实边缘或纹理时总有一帧明显领先，而没有信号的频段里所有候选都在同一水平、比值趋近 1。
比值高于 `--band-energy-gate`（默认 0.7，两侧 ±0.15 过渡）的像素才交给"邻域连贯"的那个赢家。

| 设置 | 塑料面颗粒 | 皮革 | 平坦背景 | 带内纹理@8px | @12px | 整图改动（>4 级 / 均值）|
|---|---|---|---|---|---|---|
| 参照成品 | 2.01 | 3.17 | 10.38 | 7.21 | 7.68 | — |
| 旧默认：取最大，不聚合 | 2.37 | 3.70 | 14.52 | **8.91** | **9.02** | 基准 |
| gate 0（＝处处聚合，即 `--band-energy-smooth 2`）| **1.41** | **2.94** | **11.43** | 7.59 | 7.81 | 29.2% / 3.63 |
| gate 0.6 | 1.46 | 3.06 | 12.38 | 7.95 | 8.16 | 22.9% / 3.11 |
| **gate 0.7（新默认）** | **1.54** | **3.16** | **12.85** | 8.15 | 8.35 | 18.2% / 2.70 |
| gate 0.8 | 1.69 | 3.30 | 13.38 | 8.36 | 8.53 | 12.2% / 2.13 |

这是一条**单调的取舍曲线**（`gate → 1` 就等于旧默认），没有免费午餐——但每一个操作点
都比旧默认干净，且七个区域的锐度**仍全部高于参照成品**（gate 0.7：gradE 6.037 对 5.315；
墙面 22.09 对 19.13；膝盖边 51.68 对 47.12；面部 53.34 对 52.10；平坦背景 29.88 对 28.29）。

**能量聚合在同一个"带内纹理"水平上比 `--band-power` 干净得多**（gate 0 的 1.41 对
`--band-power 2` 的 0.61 但带内 6.02），也比缩小候选窗口好：`--bracket-radius`
0/1/2/4/8/16 的颗粒是 1.23/1.53/1.71/1.93/2.17/2.37，而半径 0 会把带内纹理压到 4.51。

`--band-energy-smooth 0` **逐位**回到旧默认（已校验，最大差 0）；
`--band-energy-smooth 1`（半径 1）几乎与 2 同效；想更干净用 gate 0.5/0.6，想更接近旧观感用 0.8。

（`--band-power` 仍在，但已被这条更好的路取代：默认 0。）

#### bug 9 — 轮廓外侧的亮环（"光晕"）：上侧越界

**症状**：主体轮廓外面有一圈**比背景更亮**的细环（2–6 像素宽），远近交界处最明显。

**判据（硬）**：正确融合的每一个输出值都必须落在**参与融合的那些帧在该像素的取值范围**内。
新增 `--save-range <prefix>` 把夹取用的 **warped** 逐像素 lo/hi 导出来——**只有它是权威**：
帧间位移在细浮雕尺度上就有 ±10 级，用未 warp 的帧范围会高估（本项目为此一度误判）。

| 画布 x（y=2850，轮廓在 ~2815） | 帧最小值 | 帧最大值 | 旧默认 | 上一版 | **两侧都夹** |
|---|---|---|---|---|---|
| 2800 | 172 | 190 | 200.6 (+10.5) | 196.3 (+6.0) | ≤190 |
| 2804 | 167 | 192 | 207.4 (+15.1) | 201.9 (+9.6) | ≤192 |
| 2812 | 157 | 199 | 204.9 (+5.7) | 200.1 (+0.9) | ≤199 |

（括号内是超过该点**所有帧最大值**的量。）

**机理**：和 bug 6 是同一枚硬币的两面。拉普拉斯金字塔**逐频段取最大**时，高对比边缘上
亮侧的过冲与暗侧的负冲来自不同帧，相加后就落在所有帧的取值范围之外。对照实验：
`--mode max`（纯逐频段取最大）超出得更多（+20…+26），**`--mode pyramid`（纯深度插值）完全不超**。
这正是文献与 Zerene 文档对 PMax 类算法的描述：锐，但有光晕。

**修法**：`--clamp-hi` **默认开启**（回到"两侧都夹"；`--no-clamp-hi` 可关）。

| | 剪影带内 超出帧范围 >8 级 | 带内纹理@8px | gradE | 假黑 L<5 |
|---|---|---|---|---|
| 旧默认（不夹上侧） | 14.2% | 8.91 | 6.701 | 0.3428% |
| 上一版（不夹上侧） | 8.3% | 8.15 | 6.037 | 0.2869% |
| **两侧都夹（新默认）** | **5.4%** | **7.27** | 5.930 | 0.2869% |
| 帧自身能提供 / 参照成品 | — | 7.21 / 7.21 | 5.315 | — |

**这一条更正了 ⑧ 之前的结论**：当时认为"上侧那点越界正是远侧表面的浮雕，夹了就成灰带"——
**是错的**。带内纹理 8.91 比**帧自身能提供**的 7.21 高 24%，也就是说被夹掉的正是过冲。
当时看到的"灰带"另有原因（深度斜坡 + 候选窗口过宽），那个原因在之后已经修掉，
所以现在夹上侧不会再产生灰带。`--clamp-hi-dilate`（膨胀上界）实测无收益，默认 0 保留。

#### bug 10 — 轮廓外那条亮带：粗层也用宽候选窗口

**症状**：主体轮廓外面贴着一条**比背景墙更亮**的带，宽约 20–40 px，形状完全跟随轮廓
（用户画的标记就落在这条带上）。它和 ⑨ 的"亮环"是两回事：⑨ 只有 2–6 px 宽、是越界；
这条是十几像素到几十像素宽、且**并没有越出帧的范围**，所以 `--clamp-hi` 只能砍掉一半。

**判据**：不能拿"墙的远端"当基准——墙自己有慢变亮度梯度，越靠近主体越暗。
本轮改成两条：
1. `tools/banddelta.py` —— 以**墙面合焦的那一帧**同位置作基准；
2. `tools/bandtruth.py` —— 以**逐像素取深度对应帧**的参照作基准（墙是斜面，
   合焦帧在标记区内会漂十几帧，单帧不能当真值）。

实测（标记区，轮廓外 4–22 px 平均）：

| 方案 | 带内亮度 − 逐像素参照 | 带内/远端纹理比 |
|---|---|---|
| 旧默认（所有层宽窗口） | **+2.81** | 0.865 |
| `--coarse-levels 2` | +2.08 | 0.901 |
| `--coarse-levels 3` | +1.39 | 0.929 |
| **`--coarse-levels 4`（新默认）** | **+0.93** | **0.933** |
| 纯深度插值（该指标的地板） | +0.86 | 0.804 |
| 单帧参照本身 | 0 | 0.766 |

**机制**：宽候选窗口（`--bracket-radius 16`）是为了救"深度被读错"的细浮雕而存在的，
但它同时让**邻面离焦时的泛光**进入候选——而泛光是个**粗尺度**特征，于是它在最粗的
几层里赢得逐频段最大值，把轮廓外那一圈墙染亮。`--mode max` 把它放大到 +11.4 级，
`--mode pyramid`（完全不取最大）是 +0.86（地板）。

**修法**：`--coarse-levels 4` **默认**，即金字塔最粗的 4 层改用 `--bracket-radius-coarse 0`
（只用夹住深度的那两帧，与基带一致），**细节层仍保留 ±16 的宽窗口**——因为救浮雕的能力
在细节层，实测 `--coarse-levels 6` 会让平坦背景的锐度掉一成（28.82 → 25.56），
而 4 与 3 几乎无差别。代价与收益：gradE 5.833 → **5.967**（升），
带内纹理比 0.865 → **0.933**（升，且单帧参照只有 0.766），六区域锐度全部仍高于 AF 成品。

**顺带纠正一个我先前用错的判据**：我一度用"带内/远端纹理比 < 1"断定轮廓外的浮雕被抹掉了。
用**逐像素参照**去量才知道：那个带在单帧里本来就只有远端的 0.766，我们做到 0.865–0.933
是**比单帧更好**。0.865 看起来"低"，只是因为分母（远端）在同一张图里被抬高了。
凡是"局部/全局"的比值判据，都必须先确认分母没有跟着被改动。

#### bug 11 — 亮带的根因：轮廓外的深度被主体边缘带偏（"光带"的真正来源）

bug 10 只修掉了亮带的一半。用户看完 ⑩ 的成品后说：**"带还在，不过从纯色变成了一些渐变过渡，
可以看到一些淡淡的墙面纹理了"**——这句话把方向指对了：颜色/亮度带仍在，只是变软了。

**为什么会得出"已经修好"的错误结论**：⑩ 的判据拿**工具自己存的深度图**去挑参照帧。
如果深度在轮廓附近本来就是错的，参照会以同样的方式错，两者相减自然接近 0
（当时报 +0.93，还以为是地板）。**参照不能由被测对象自己生成。**

**独立真值**：`tools/depthtruth.py` 用**把主体像素排除出测度窗口**的焦点响应重新取 argmax
（8×8 单元）。这样主体那条强边缘无法投票，赢下来的帧按构造是"墙真的合焦"的帧。

结果（标记区，非 33 即 25 → 见下）：

| 距轮廓 | 单元数 | 真值帧 | 工具深度 | 差 |
|---|---|---|---|---|
| 2–8 px | 357 | 33.0 | 25.8 | **−7.2** |
| 8–16 px | 551 | 33.0 | 31.8 | −1.2 |
| 16–24 px | 499 | 33.0 | 32.9 | −0.1 |
| 24 px 以外 | 3000+ | 33.0 | 33.0 | −0.0 |

**机制**（`src/main.rs` 里早就写着这段，只是那段代码被默认关掉了）：焦点响应是**方框**窗口
聚合的，窗口一旦横跨主体的硬边缘，边缘那个强得多的响应就主导了和，于是**深度台阶落在
远侧表面内部约一个窗口半径处**。那一圈墙因此拿到主体的深度，被从"墙是虚的"那些帧里取
亮度——这就是亮带，而且它是渐变的（越靠近轮廓越偏），方向也解释了为什么它总是偏亮：
离焦的墙恰好更亮。

**修法**：`--edge-aware 16` **默认开启**（＝再跑一次 argmax，但响应先按图像本身加权聚合，
权重只依赖 guide、不依赖帧号，所以不违反 bug 4 的"测度里不许有逐帧自适应的量"）。
实测：

| | 轮廓外 2–8 px 深度误差 | 带内亮度超出真值（4–24 px） | 8 px 处 |
|---|---|---|---|
| 关（旧默认） | −5.0 帧 | +2.36 | +4.4 |
| `--edge-aware 12` | — | +0.54 | +1.4 |
| **`--edge-aware 16`（新默认）** | **−0.6 帧** | **+0.22** | **+0.9** |

按距离看（相对真值，级）：旧默认 +7.6 / +5.5 / +3.3（4/8/12 px），
新默认 **+1.5 / +0.2 / −0.3** —— 8 px 以外已经压在 0 上。
六区域锐度五处变高（wall@33 20.85→21.87、knee-edge 49.78→51.34、smooth-body 50.44→51.48、
leaf 21.93→22.27、face 52.86→53.23），**平坦背景掉 0.9%**（28.08→27.84，AF 成品 28.29）。

**两个开关是两件事，缺一不可**：把深度改对之后，如果候选窗口仍然宽，逐频段最大值照样会
把邻面的泛光挑进来——实测 `--edge-aware 16 --coarse-levels 0` 是 **+3.27**，
两个都开才是 +0.22。⑩ 治"选帧"，⑪ 治"深度"。

#### bug 12 — 轮廓外的彩色渗出：近景把颜色染到远处的表面上

**症状**：轮廓外面紧贴着一圈**带主体颜色**的晕（约 15–20 px 宽）。手办对着白墙拍，
红色花瓣外就有一圈暗红。

**判据**：沿轮廓法线取 R-G（红-绿）剖面。这是唯一能看见它的量——**亮度**上它几乎无痕
（轮廓外亮度本来就在帧范围内，见 ⑨）。

| 离花瓣 | 4 px | 6 px | 8 px | 10 px | 12 px | 16 px |
|---|---|---|---|---|---|---|
| 旧默认 | 24.1 | 24.1 | 22.7 | 18.7 | 13.4 | 5.7 |
| **栈里的每一帧** | 36–44 | 25–39 | 17–31 | 8–25 | 5–20 | 3–9 |
| 人工修过的参照成品 | 9.8 | 8.1 | 8.7 | 9.4 | 7.4 | 2.9 |

**每一帧都在 36–52，我们原来 24，而参照成品是 10**——这圈颜色是**离焦把花瓣的光摊到墙上**
的结果，光学上真实存在，而人工精修的那张是把它**擦掉**了。所以这不是算法缺陷，是"要不要做
修图那一步"的取舍；用户要求去掉，于是有了下面这一步。

**修法**：`--spill 20`（**默认开**，`--spill 0` 关闭）。判据**只用深度场、完全不看颜色**，
所以背景是白墙、绿布还是木桌都一样：

1. 深度梯度定出剪影（细线。**不能用"局部深度跨度"**——那会摊成几十像素宽的带，带内就没有
   "离剪影够远"的参考像素了，整个 pass 会静默地什么都不做）；
2. 只动**剪影外侧**、且位于深度台阶**远侧**（= 不是主体本身）的像素，作用范围 `radius`；
3. 替换色取自"**离剪影够远**（> radius）且同样在远侧"的像素的局部平均——那才是那个表面自己的
   颜色。只改**低频颜色**（偏移量在 1/4 分辨率上算完再**双线性**插值），纹理与细节分毫不动。

| | 4 px | 6 px | 8 px | 10 px | 12 px | 16 px |
|---|---|---|---|---|---|---|
| **本版（spill 20）** | **8.5** | **4.2** | **4.1** | **3.7** | **3.0** | **0.9** |
| 人工修过的参照成品 | 9.8 | 8.1 | 8.7 | 9.4 | 7.4 | 2.9 |

**代价**：六个参照区域里膝边 −0.8%、光滑身体 −0.7%、叶片 −2.0%，墙面与面部不变，
**gradE 5.969 → 6.006（升）**，六区域仍全部高于参照成品。

**两个设计上的坑（都记下来）**：
- **不能按"同一深度切片"取替换色**。晕就贴在后面那个表面上，所以"晕"和"它的正确颜色"在同一个
  切片里：排除晕，样本就没了；不排除，平均又被晕拉回去。可行的判据是**离剪影的距离**。
- **1/4 分辨率上的修正必须双线性上采样**。用最近邻会印出 4 px 的色块，平坦背景的锐度会从
  27.8 **虚升**到 42.7（那不是变清楚，是多了格子）——"锐度异常升高"比"变模糊"更容易骗过人。

**过程中的两个"条件恒假"（都靠"输出与默认逐位相同／md5 一致"才暴露）**：
- `util::box_blur` 返回的是**平均值**（0..1），而它被拿去跟像素数（`4.0`）比 → 参考色全 NaN、
  整个 pass 静默失效。
- 打补丁的脚本只替换**第一处**匹配，`count == 2` 的断言却通过了；另一次脚本中途抛异常，
  **文件根本没写**，于是"跑了 8 分钟"的其实还是旧二进制。
  **判据：比对输出 md5，或看日志里有没有新版才有的输出。**


#### 关于"可靠性判据"的更正

上面那张"所有判据都失败"的表是在这几个 bug 之上测的，**结论不再成立**。
一个正面例子是 `--focus-radius` 与 `--analysis-scale` 的配合——
见下面第 4 节关于分析分辨率的说明。

另一个被推翻的前提是"**我们的输出比参照成品噪**"。它来自一个直觉推理：
逐频段取最大是极值统计量，所以在平坦区必然放大噪声。之前一直没验证过——
因为用来"验证"的判据（整块背景的纹理 std、高频能量）**同时包含真实细节**。

现在直接测**全图最平坦的那块 200×200**（在参照成品上按梯度能量找出来），
用"该区域与其自身 3×3 中值之差"作为颗粒度：

| 版本 | 全图颗粒度 | **最平坦区域颗粒度** |
|---|---|---|
| AF导出 | 3.328 | 1.400 |
| **mukastack 默认（逐频段取最大）** | 3.990 | **1.332** |
| `--band-power 2`（能量加权平均） | 2.928 | 0.470 |
| `--band-power 4` | 3.476 | 0.760 |

**在最平坦的区域我们比参照成品更干净**（1.332 对 1.400）。
所以全图上多出来的那 20% 高频能量是**真实细节**，不是噪声——
这也解释了为什么 `--band-power 2/4` 虽然把"噪声"指标压得很好看，
却把每个区域的轮廓锐度都拉到参照成品附近或以下（面部 45.7 / 51.0，参照 52.1）：
它压掉的正是细节。**逐频段取最大保持为默认。**

#### 更正：那块背景墙从来不是弱项（是我的测量脚本错了）

上一轮这里写着"紧贴主体的背景墙仍比参照成品软：轮廓锐度 16.6 对 19.1"。
**那个数字是错的。** 输出图是画布的裁切，裁切位置**每次运行都会变**（对齐的有效区域不同），
而我的对比脚本把参照裁切的原点**硬编码**成了 `(1288, 488)`；实际已经漂到 `(1288, 496)`。
于是它一直在比较**纵向偏 8 像素的另一块地方**，而得出的正是"墙比参照软"这种似是而非的结论。

用自动定位原点重新测（`tools/patchlib.py`，它自己找原点）：

| 区域 | AF导出 | mukastack |
|---|---|---|
| 背景墙 | 19.13 | **23.33** |
| 膝盖边缘 | 47.12 | **56.27** |
| 光滑身体 | 47.52 | **56.65** |
| 底座叶片 | 20.90 | **24.44** |
| 面部 | 52.10 | **58.23** |
| 平坦背景 | 28.29 | **32.14** |

**每一个区域都高于参照成品**，包括那块墙。这一段历史结论全部作废：
"7.2 → 12.9 → 16.6 的改进轨迹"是同一条坐标漂移上的三次测量。

**教训**：输出图的裁切原点不是常量。任何跨版本的逐区域对比都必须先定位原点，
哪怕只是差 8 个像素——8 像素足以把"比参照软"和"比参照锐"颠倒过来。

#### `--depth-fix` 作为兜底保留

专业软件（Helicon Focus、Zerene Stacker，以及 Affinity 的克隆修补）都提供深度图手工修饰，
本工具保留同样的能力：

```bash
# 把输出图 (2695,1488) 起 500x500 的区域强制用第 6 帧
mukastack ./stack -o merged.png --depth-fix 2695,1488,500,500,6
```

`x,y,w,h,frame`，坐标为**输出图**像素，可重复。矩形内所有像素都用同一帧，
框内背景会一起去掉纹理 —— 把框收到紧贴物体即可。

#### 参数层面已经到顶（负面结果，别再重复）

| 试过 | 结果 |
|---|---|
| `--focus tenengrad --focus-prefilter 0` | 低对比区对真实锐度的排序相关性 0.895 对 0.549、动态范围 3.1× 对 1.5×，明显更好——但端到端 gradE 6.747 对 6.757、各区域轮廓锐度**逐位相同** |
| `--focus-radius 32`（窗口 65 像素） | 中性 |
| `--base-winner 0.5 / 1.0`（基带也参与仲裁） | 中性；因为最粗层只含极低频，而**离焦几乎不改变极低频** |
| `--denoise 0.08 / 0.15` | gradE 6.28 / 5.87，各区域锐度全线下降，而背景 std 只从 12.93 降到 12.32 |
| `--band-power 2 / 4 / 8` | 见上文：是一根**单调的取舍曲线**（P2 最干净但细节也最少，P8 拿到最大值的 96% 细节、少 6% 颗粒）。默认仍取最大值，因为要的是细节。⑧ bug 8 给出了它在**光滑主体面**上的完整曲线（塑料面颗粒 0.39/0.61/1.13 对 2.37）|
| `--bracket-radius 0 / 2 / 4 / 8 / 16` | 深度修好之后，K≥4 即进入平台（墙面 23.18 / 23.35 / 23.33）；K 保留 16 是为了那些**真的没有焦点信息**的表面 |
| `--mode max`（完全不做深度约束） | gradE 6.972 对 6.902，但**背景墙 22.38 对 23.33**，颗粒度 4.228 对 4.125；深度约束现在确实在起作用 |

原因：宽仲裁窗口已经在替深度场兜底。

#### 对齐：修好之后反而略"软"，但保留

同一组素材，关掉几何对齐（`--no-align`）测出来**更高**：
gradE 7.194 对 6.902、墙面 25.73 对 23.33、颗粒度 4.662 对 4.125。

原因是清楚的：对齐要把每帧重采样（这组素材有真实的 0.9% 对焦呼吸，
各帧所需的缩放/平移不同），而重采样本身是一次轻微的**低通**；
不对齐时每一帧的原始像素原封不动，代价是帧间有 ≤1.5 像素的失配。

在**这块场景**里，失配的代价小于重采样的代价——因为画面的四角都是**纹理**，
而纹理的统计特性在失配下几乎看不出差别（四角逐一放大对比确认：没有重影、没有撕裂）。
但一个主体延伸到画面边缘的场景就不是这样了：失配会在选择边界上留下几何错位。
所以**默认保留对齐**，这个取舍记录在此。

（如果只想要这一块场景的最大锐度，`--no-align` 是可用的。
代价是画面边缘可能出现错位，需要自己目视确认。）

#### 关于 GPU

瓶颈不在算力：50 帧 33MP 约 120 秒（融合占约 60 秒），已 rayon 全核并行。
这一版的所有改进都是**算法正确性**，加 GPU 不会带来任何画质提升。
要提速的话，融合阶段最容易移植（逐帧独立、纯卷积）。

---

## 复盘：值得记住的实现坑

按"症状 → 原因"排，全部是本项目真实踩过的。

1. **整幅图均匀变软** — 重采样的半像素约定。
   像素 `i` 的中心在连续坐标 `i + 0.5`；重采样时少了这个 `-0.5`，每帧会被平移半个像素
   并做一次双线性低通。修复后相对参照图的梯度比 0.710 → 0.834。

2. **整幅图均匀变软（其二）** — 重采样核。
   亚像素位移下双线性等价于 2 抽头盒滤波，高频损失很大，换成 Catmull-Rom 后细节回来。

3. **某个方向的帧系统性胜出，与真实对焦无关** — 焦点测度测在了重采样过的像素上。见 ⑤ bug 2。

4. **对齐搜索"越走越偏"，NCC 随离参考帧的距离崩塌** — 注册特征与被测量本身相关。
   梯度幅值随对焦变化，于是缩放搜索在匹配模糊。见 ⑤ bug 1。

5. **平坦区域被判成"第一帧"并成片扩散** — 无信息像素也在投票。见 ⑤ bug 3。

6. **"百分位掩膜 + 补洞"处理不可靠像素是错的。**
   百分位**必然**让固定比例的像素被替换，不管它们是否真的不可靠：
   实测让 5% 的像素深度被改动超过 11 帧，一片真实深度 29.8 的树脂叶片被填成 8.0。
   反过来，任何"绝对阈值"也必须建立在**原始量**上，而不是邻域聚合过的量上
   （见 ⑤ bug 3）。

7. **指标要和目视一致，且一次只量一件事。**
   "细节能量"把细节和颗粒混在一个数里，于是既不能说清"变锐了"也不能说清"变噪了"。
   把它们分开量——锐度看**最强边缘的梯度峰值**，噪声看**最平坦区域**的颗粒度——
   结论才稳定。本项目为此反复被带偏过好几轮。
   留一块对照区域（比如面部）也很有用。

8. **改参数之前先确认管线是对的。**
   本项目早期有一整轮调参（`--focus-radius`、`--analysis-scale`、预滤波、大半径引导滤波）
   是在上面这些 bug 之上做的，结论全部作废——有些方向修好之后正好反过来
   （比如"全分辨率分析反而更差"）。

9. **逐区域的对比必须自己定位裁切原点。**
   输出图是画布的一个裁切，裁切位置**每次运行都会变**。把原点硬编码进脚本
   （本项目一度写成 `(1288, 488)`，实际已经漂到 `(1288, 496)`）会让脚本静默地
   比较**另一个地方**，并给出"某个区域比参照软"这种看似合理的错误结论。
   8 个像素足以把结论颠倒。用 `tools/patchlib.py` 的 `locate()`，它自己找。
   同理，**任何"某处模糊"的报告，先确认坐标是哪个图的坐标系**：
   输出图 / 参照裁切图 / 原始画布，三者两两差一个偏移。

10. **测度里不允许出现任何逐帧自适应的量。**
    焦点测度要跨帧比较，所以凡是"每帧各算一次"的东西——归一化、灰度转换、
    直方图拉伸、自动曝光——都会把该帧的其它属性混进"清晰度"里。
    本项目为此付出过两次代价：对齐特征（见 ⑤ bug 1）和灰度转换（见 ⑤ bug 4）。
    两次的表现形式相同：**某些帧系统性胜出，与真实对焦无关**，
    而受害者总是低信噪比的区域。
